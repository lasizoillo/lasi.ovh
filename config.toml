# The URL the site will be built for
base_url = "https://lasi.ovh"

# The site title and description; used in feeds by default.
title = "La web del lasi"
description = "Pedradas sobre tecnología, recetas y lo que me de la venada."

# Whether to automatically compile all Sass files in the sass directory
compile_sass = true

# Whether to build a search index to be used later on by a JavaScript library
build_search_index = true

theme = "anpu"

taxonomies = [
    { name = "categories", rss=true },
    { name = "tags", rss=true },
    { name = "projects", rss=true},
]

[markdown]
# Whether to do syntax highlighting
# Theme can be customised by setting the `highlight_theme` variable to a theme supported by Zola
highlight_code = true

render_emoji = true


[extra]
# Put all your custom variables here
anpu_menu_links = [
    { url = "$BASE_URL/ideas/", name = "Ideas" },
    { url = "$BASE_URL/proyectos/", name = "Proyectos" },
    { url = "$BASE_URL/blog/", name = "Blog" },
#    { url = "$BASE_URL/about/", name = "About" },
    { url = "$BASE_URL/tags/", name = "Tags" },
]

anpu_date_format = "%d/%m/%Y"
